apiVersion: v1
kind: Service
metadata:
  name: {{ template "azureexampletutorial.fullname" . }}
  labels:
    app: {{ template "azureexampletutorial.name" . }}
    chart: {{ template "azureexampletutorial.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "azureexampletutorial.name" . }}
    release: {{ .Release.Name }}
