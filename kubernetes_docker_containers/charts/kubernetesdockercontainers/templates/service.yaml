apiVersion: v1
kind: Service
metadata:
  name: {{ template "kubernetesdockercontainers.fullname" . }}
  labels:
    app: {{ template "kubernetesdockercontainers.name" . }}
    chart: {{ template "kubernetesdockercontainers.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "kubernetesdockercontainers.name" . }}
    release: {{ .Release.Name }}
